{
  "name": "react-crud-app",
  "version": "1.0.0",
  "engines": {
    "node": "16.13.2"
  },
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server.js",
    "build": "cd crudapp && npm run build",
    "install-client": "cd crudapp && npm run install",
    "client": "npm start --prefix crudapp",
    "heroku-postbuild": "npm run install-client && npm run  build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "concurrently": "^7.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "mongoose": "^6.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
